/**
 * Fiber AI TypeScript SDK — Zod schemas
 *
 * Runtime validation schemas auto-generated from the OpenAPI spec.
 * Imported via the `@fiberai/sdk/zod` subpath so apps that don't need
 * runtime validation pay zero bundle cost.
 *
 * @example
 *   import { zPeopleSearchData } from '@fiberai/sdk/zod';
 *   const validated = zPeopleSearchData.parse(rawInput);
 */

export * from "./generated/zod.gen.js";
